name: stw-mapping-library-ci


resources:
  repositories:
  - repository: stw-mapping-library 
    type: github
    endpoint: DEFRA
    name: DEFRA/stw-mapping-library

trigger: none

pool:
  vmImage: 'ubuntu-latest'

steps:
  - checkout: self
  - checkout: stw-mapping-library

  - task: PowerShell@2
    name: mvn
    displayName: mvn clean install
    inputs:
      pwsh: true
      azurePowerShellVersion: latestVersion
      targetType: "filePath"
      filePath: $(Agent.BuildDirectory)/s/stw-ops/scripts/mvn-build.ps1
      arguments: -Tag ${{ parameters.jenkinsBuildID }}
      workingDirectory: "$(Agent.BuildDirectory)/s"

# - task: Maven@3
#   inputs:
#     mavenPOMFile: '/home/vsts/work/1/s/stw-mapping-library/pom.xml'
#     publishJUnitResults: false
#     javaHomeOption: 'JDKVersion'
#     mavenVersionOption: 'Default'


#"Execute Maven build", script: "mvn -Drevision=$BUILD_VERSION -U -T 1C clean package install --settings ./settings.xml --no-transfer-progress -DskipTests")